#include <stdio.h>
#include <stdlib.h>

int main(){
	printf("Hello World!\n");
	int xs size = 0;
	int* xs = malloc(sizeof(int) * xs_size);
	xs[0] = 88;
	for (int i=1; i < xs_size; ++i0{
		xs[1] = xs[0] * 1;
	}
	free(xs);
	return 0;
}
